# Simon Memory — 2026-03-16

## Flobase Portal — Full Incident Response

### Root Cause
`flobase-api-handler` deployed with 3KB zip (no psycopg2). All 7 endpoints dead.

### What Was Fixed
1. **Lambda redeployed** — 21MB zip with full deps. All endpoints → HTTP 401 (healthy)
2. **DB indexes added:**
   - `idx_clients_dsc_enrolled` (dsc_id, enrolled_date WHERE is_latest_record=true)
   - `idx_tranche_dsc_status` (dsc_id, status)
   - `idx_clients_first_payment` (first_payment_clear_date WHERE is_latest_record=true)
   - `idx_clients_eligible` (dsc_id, eligible, tranche_id WHERE is_latest_record=true)
3. **v_portfolio_summary** — converted from regular view → materialized view
4. **EventBridge rule** — `flobase-portfolio-matview-refresh` fires every 15 min → REFRESH CONCURRENTLY
5. **flobase-db-migrate Lambda** — created and deployed (VPC, same role as api-handler)
6. **handle_portfolio fast path** — uses matview when no date filter, ~50ms vs 2.8s
7. **Lambda redeployed** after code change

### Cost Optimization Applied
- `openclaw.json` updated: Simon forced to `local/qwen2.5-coder:32b` primary
- Sonnet fallback now has `cacheRetention: long` + `cache: true`
- `AGENTS.md` updated with strict routing table and cost targets

### Coordination
- Optimus handled: frontend fix (dead Next.js route), status page false-alarms, Cognito IAM, S3/CloudFront deploy
- Simon handled: Lambda, RDS, indexes, matview, EventBridge, cost config

### Files Changed
- `~/.openclaw/workspace/flobase-capital-platform/backend/lambdas/api-handler/lambda_function.py` — handle_portfolio fast path
- `~/.openclaw/openclaw.json` — cache + routing config
- `~/.openclaw/workspace/simon/AGENTS.md` — strict model routing
- `playbooks/lambda-deploy.md` — new playbook

### AWS Resources Created
- Lambda: `flobase-db-migrate`
- EventBridge rule: `flobase-portfolio-matview-refresh` (rate 15 min)
- DB indexes: 4 new indexes on clients + tranche_master
- Matview: `v_portfolio_summary` (was regular view)
